To access MySQL database in terminal:

```
mysql -uroot --host=localhost --port=33061
```

To connect MySQL database via SequelPro:

host:localhost
user:root
password:
port:33061

_then select `connect via socket`_
